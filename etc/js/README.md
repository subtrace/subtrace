This package contains the JS bindings for Subtrace. Here's how you can get started using Subtrace:

1. Install Subtrace using `npm install subtrace`.

2. Import it at the top level: `import "subtrace"`

3. Start your server the way you normally would.